<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp">

    <Button
        android:id="@+id/btnRestaurant"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:backgroundTint="@color/verde"
        android:fontFamily="serif"
        android:textColor="@color/white"
        android:textSize="18sp"
        android:textStyle="bold"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/btnEditReview"
        android:layout_marginEnd="8dp"
        tools:text="Restaurant Tokyo" />

    <LinearLayout
        android:id="@+id/llStars"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="8dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/btnRestaurant">
    </LinearLayout>

    <TextView
        android:id="@+id/tvReviewText"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:fontFamily="serif"
        android:textSize="16sp"
        app:layout_constraintTop_toBottomOf="@id/llStars"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        tools:text="Molt bona atenció i menjar excel·lent. Totalment recomanat si busques opcions segures." />

    <ImageButton
        android:id="@+id/btnEditReview"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:src="@drawable/baseline_edit_24"
        android:background="?attr/selectableItemBackgroundBorderless"
        app:tint="@color/verde"
        android:contentDescription="@string/editar_ressenya"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="@id/btnRestaurant"/>

</androidx.constraintlayout.widget.ConstraintLayout>
